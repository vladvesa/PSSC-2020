using Access.Primitives.IO;
using System;
using System.Collections.Generic;
using System.Text;
using static PortExt;

namespace StackUnderflow.Domain.Core.Contexts.FrontOffice
{
    public static class QuestionsDomain
    {
        //public static Port<ICreateQuestionResult> CreateQuestion(string title, string body) =>
        //    NewPort<CreateQuestionCmd, ICreateQuestionResult>(new CreateQuestionCmd(title, body));

        //public static Port<ICreateAnswerResult> CreateAnswer(int tenantId, Guid userId, int questionId, string answer) =>
        //    NewPort<CreateAnswerCmdInternal, ICreateAnswerResult>(new CreateAnswerCmdInternal(tenantId, userId, questionId, answer));
    }
}
