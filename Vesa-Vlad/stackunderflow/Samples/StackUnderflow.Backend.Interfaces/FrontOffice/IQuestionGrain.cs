using Orleans;
using StackUnderflow.Backend.Abstractions.Responses;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace StackUnderflow.Backend.Abstractions.Fontoffice
{
    public interface IQuestionGrain : IGrainWithStringKey
    {
    //    Task<CreateAnswerResponse> CreateAnswer(Guid userId, int questionId, string answer);
    //    Task<VoteResponse> VoteUp(Guid userId);
    //    Task<VoteResponse> VoteDown(Guid userId);
    //    Task<VoteResponse> CancelVote(Guid userId);
    //    Task MarkAsViewed(Guid userId);
    }
}
